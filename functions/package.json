{
  "name": "ketering-functions",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint . --ext .ts"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "cloudFunctions": {
    "runtime": "nodejs20",
    "deployment": "gen1"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.11.6",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
